[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200

# This lesson's tests are intentionally forge-std-free so the workspace stays
# offline-runnable. See test/WalrusAnchor.t.sol for the inline cheatcode
# interface.
