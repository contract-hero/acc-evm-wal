[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

# Forge-std-free so the lesson workspace stays offline-runnable. The test file
# inlines a minimal Vm cheatcode interface — see test/WalrusResolver.t.sol.
